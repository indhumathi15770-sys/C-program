#include <stdio.h>
int main() {
    int a[7], max, min;
    for(int i=0;i<7;i++){
        scanf("%d",&a[i]);
    }
    max = min = a[0];
    for(int i=1;i<7;i++){
        if(a[i]>max) max=a[i];
        if(a[i]<min) min=a[i];
    }
    printf("Highest = %d, Lowest = %d", max, min);
    return 0;
}
