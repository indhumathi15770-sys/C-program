#include <stdio.h>
int main() {
    int units;
    float bill;
    printf("Enter units: ");
    scanf("%d", &units);
    bill = units * 5; // per unit 5 rupees
    printf("Electricity Bill = %.2f", bill);
    return 0;
}
