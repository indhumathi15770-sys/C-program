#include <stdio.h>
int main() {
    int n, a[50];
    printf("Enter number of students: ");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        printf("Enter mark %d: ", i+1);
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++)
        printf("Mark %d = %d\n", i+1, a[i]);
    return 0;
}
