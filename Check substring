#include <stdio.h>
#include <string.h>
int main() {
    char s[100], sub[50];
    gets(s);
    gets(sub);
    if(strstr(s, sub))
        printf("Substring Found");
    else
        printf("Substring Not Found");
    return 0;
}
