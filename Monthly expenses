#include <stdio.h>
int main() {
    float exp, total=0;
    for(int i=1;i<=30;i++){
        scanf("%f",&exp);
        total += exp;
    }
    printf("Total Monthly Expense = %.2f", total);
    return 0;
}
